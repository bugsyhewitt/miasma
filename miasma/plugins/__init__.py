"""Plugin package.

Drop a new plugin here as ``<cve-id>.py`` with a module-level ``metadata: dict``
and a ``probe(target: Target) -> Finding | None`` function. See
``test_always_finds.py`` for the minimal shape.
"""
